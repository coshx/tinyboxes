defaults: &defaults
  adapter: postgresql
  username: <%= ENV['USER'] %>

development:
  <<: *defaults
  database: unicef_tinyboxes_development

test:
  <<: *defaults
  database: unicef_tinyboxes_test

production:
  <<: *defaults
